---
import Layout from '../layouts/Layout.astro';
import { fetchProducts } from '../utils/index';

// Fetch real data from Tribute API
const products = await fetchProducts();
const digitalProducts = products.filter(p => p.type === 'digital');
const physicalProducts = products.filter(p => p.type === 'physical');

// Calculate totals for display
const totalDigitalValue = digitalProducts.reduce((sum, p) => sum + (p.amount / 100), 0);
const totalPhysicalValue = physicalProducts.reduce((sum, p) => sum + (p.amount / 100), 0);
---

<Layout 
  title="Crypto Crowdfunding - Support Rediant Art Innovation"
  description="Join Rediant's crypto crowdfunding campaigns and support the future of artistic jewelry. Contribute to our innovation through digital support and physical rewards."
  lang="en"
  heroImage="/images/photo_2025-09-11 00.03.32.jpeg"
  heroAlt="Crypto crowdfunding for Rediant Art"
>
  <!-- Hero Section -->
  <section 
    class="bg-image-section min-h-screen flex items-center justify-center"
    style="background-image: url('/images/photo_2025-09-11 00.03.32.jpeg')"
  >
    <div class="bg-image-content text-center px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl sm:text-6xl lg:text-7xl font-bold mb-6 fade-in-up">
          Support Rediant Art
        </h1>
        <p class="text-xl sm:text-2xl lg:text-3xl mb-8 text-neutral-100 fade-in-up">
          Help Us Build the Future of Artistic Jewelry
        </p>
        <p class="text-lg sm:text-xl mb-12 text-neutral-200 max-w-3xl mx-auto fade-in-up">
          Join our community-driven innovation through digital support and physical rewards. 
          Be part of building the most engaged artistic jewelry community worldwide.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center fade-in-up">
          <a href="#support" class="bg-[var(--color-gold)] text-white px-8 py-4 rounded-lg font-semibold hover:bg-[var(--color-charcoal)] transition-colors">
            View Support Options
          </a>
          <a href="#rewards" class="border-2 border-white text-white px-8 py-4 rounded-lg font-semibold hover:bg-white hover:text-[var(--color-charcoal)] transition-colors">
            See Rewards
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Digital Support Tiers -->
  <section id="support" class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl sm:text-5xl font-bold text-neutral-900 mb-6">
          Digital Support Tiers
        </h2>
        <p class="text-xl text-neutral-600 max-w-3xl mx-auto">
          Choose your level of support and help us build the future of artistic jewelry.
        </p>
      </div>
      
      {digitalProducts.length > 0 ? (
        <div class="grid lg:grid-cols-3 gap-8 mb-16">
          {digitalProducts.map((product, index) => (
            <div class="bg-gradient-to-br from-[var(--color-gold)] to-[var(--color-charcoal)] rounded-lg p-8 text-white">
              <div class="text-center mb-6">
                <div class="text-4xl mb-4">💎</div>
                <h3 class="text-2xl font-bold mb-2">{product.name}</h3>
                <p class="text-neutral-200">{product.description}</p>
              </div>
              
              <div class="space-y-4 mb-6">
                <div class="flex justify-between">
                  <span>Price:</span>
                  <span class="font-semibold">{(product.amount / 100).toFixed(2)} {product.currency.toUpperCase()}</span>
                </div>
                {product.starsAmountEnabled && (
                  <div class="flex justify-between">
                    <span>Stars:</span>
                    <span class="font-semibold">{product.starsAmount}</span>
                  </div>
                )}
                <div class="flex justify-between">
                  <span>Type:</span>
                  <span class="font-semibold">Digital Support</span>
                </div>
              </div>
              
              <p class="text-sm text-neutral-200 mb-6">
                Support our innovation with this digital contribution. 
                Help us build the future of artistic jewelry.
              </p>
              
              <a href={product.link} target="_blank" class="block w-full bg-white text-[var(--color-charcoal)] text-center py-3 rounded-lg font-semibold hover:bg-neutral-100 transition-colors">
                Support Now
              </a>
            </div>
          ))}
        </div>
      ) : (
        <div class="text-center py-12">
          <p class="text-xl text-neutral-600">No digital support tiers available at the moment.</p>
        </div>
      )}
      
      <!-- Physical Rewards -->
      <div class="text-center mb-16">
        <h2 class="text-4xl sm:text-5xl font-bold text-neutral-900 mb-6">
          Physical Rewards
        </h2>
        <p class="text-xl text-neutral-600 max-w-3xl mx-auto">
          Beautiful handcrafted jewelry pieces as rewards for your support.
        </p>
      </div>
      
      {physicalProducts.length > 0 ? (
        <div class="grid lg:grid-cols-2 gap-8">
          {physicalProducts.map((product, index) => (
            <div class="bg-gradient-to-br from-[var(--color-charcoal)] to-[var(--color-gold)] rounded-lg p-8 text-white">
              <div class="text-center mb-6">
                <div class="text-4xl mb-4">💎</div>
                <h3 class="text-2xl font-bold mb-2">{product.name}</h3>
                <p class="text-neutral-200">{product.description}</p>
              </div>
              
              <div class="space-y-4 mb-6">
                <div class="flex justify-between">
                  <span>Price:</span>
                  <span class="font-semibold">{(product.amount / 100).toFixed(2)} {product.currency.toUpperCase()}</span>
                </div>
                <div class="flex justify-between">
                  <span>Type:</span>
                  <span class="font-semibold">Physical Reward</span>
                </div>
                <div class="flex justify-between">
                  <span>Status:</span>
                  <span class="font-semibold capitalize">{product.status}</span>
                </div>
              </div>
              
              <p class="text-sm text-neutral-200 mb-6">
                Beautiful handcrafted jewelry piece as a reward for your support. 
                Each piece is unique and made with love.
              </p>
              
              <a href={product.link} target="_blank" class="block w-full bg-white text-[var(--color-charcoal)] text-center py-3 rounded-lg font-semibold hover:bg-neutral-100 transition-colors">
                Get Reward
              </a>
            </div>
          ))}
        </div>
      ) : (
        <div class="text-center py-12">
          <p class="text-xl text-neutral-600">No physical rewards available at the moment.</p>
        </div>
      )}
    </div>
  </section>

  <!-- Community Impact -->
  <section class="py-20 bg-neutral-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl sm:text-5xl font-bold text-neutral-900 mb-6">
          Community Impact
        </h2>
        <p class="text-xl text-neutral-600 max-w-3xl mx-auto">
          Your support directly contributes to innovation in artistic jewelry and the creation of our global community.
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="text-4xl font-bold text-[var(--color-gold)] mb-4">{digitalProducts.length}</div>
          <h3 class="text-xl font-semibold text-neutral-900 mb-2">Digital Support Tiers</h3>
          <p class="text-neutral-600">Available for community support</p>
        </div>
        
        <div class="text-center">
          <div class="text-4xl font-bold text-[var(--color-gold)] mb-4">{physicalProducts.length}</div>
          <h3 class="text-xl font-semibold text-neutral-900 mb-2">Physical Rewards</h3>
          <p class="text-neutral-600">Handcrafted jewelry pieces</p>
        </div>
        
        <div class="text-center">
          <div class="text-4xl font-bold text-[var(--color-gold)] mb-4">∞</div>
          <h3 class="text-xl font-semibold text-neutral-900 mb-2">Community Members</h3>
          <p class="text-neutral-600">Growing global community</p>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  .fade-in-up {
    animation: fadeInUp 0.8s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
